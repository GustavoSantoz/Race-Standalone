-- Configurações de distância e aparência dos checkpoints
CHECKPOINT_PROXIMITY_THRESHOLD = 10.0 -- Distância para considerar que o checkpoint foi passado (em unidades)
CHECKPOINT_BLIP_COLOR = 5 -- Cor do blip (verde)
CHECKPOINT_BLIP_SCALE = 0.8 -- Tamanho do blip
CHECKPOINT_BLIP_SPRITE = 1 -- Sprite do blip (marcador padrão)

-- Configurações de cor e tamanho do checkpoint 3D
CHECKPOINT_3D_COLOR = {
    r = 0,
    g = 255,
    b = 0,
    a = 100
} -- Cor do checkpoint 3D (verde)
CHECKPOINT_3D_SIZE = 3.0 -- Tamanho do marcador 3D

-- Tecla para adicionar um checkpoint (F10)
ADD_CHECKPOINT_KEY = 57